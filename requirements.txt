# PyTorch CUDA wheels (CUDA 13.0)
--extra-index-url https://download.pytorch.org/whl/cu130
# basic dependencies
torch==2.9.1
torchvision==0.24.1
datasets==2.21.0
transformers==4.57.3
tokenizers>=0.22.0,<0.24
deepspeed==0.15.4
accelerate==1.3.0
peft==0.14.0
timm==1.0.3
numpy==1.26.4
# data processing
decord==0.6.0
imageio==2.34.0
imageio-ffmpeg==0.4.9
moviepy==1.0.3
opencv-python==4.10.0.84
pyarrow
pysubs2
ffmpeg-python
# misc
scikit-learn==1.4.2
huggingface_hub>=0.34.0,<0.44
sentencepiece==0.2.0
shortuuid
einops==0.6.1
einops-exts==0.0.4
safetensors>=0.4                     # Stage-2 qbase warm-start reads token_compressor.* from HF shards
# Stage-2 segment fold (videollama3/model/segment_aggregator.py) runs on a pure-PyTorch
# SSD scan by default -- no extra dep needed, safe to skip. If both of the below are
# importable, Mamba2Mixer.forward() (CUDA only) auto-swaps in the fused causal_conv1d +
# mamba_ssm.ops.triton.ssd_combined kernels (verified to agree with the reference to
# bf16 noise). The Triton kernels only pay off at batch>=8 (~3.8-4x; a wash at batch=1,
# launch overhead ~= the reference einsum path there) -- compress_windows() in
# compressor.py batches every unit in one call (front-padded + dt-masked, arbitrary
# per-unit N_u) into ONE SegmentAggregator call instead of one per unit, so real Phase-2
# steps (per_device_train_batch_size videos x up to stage2_max_units each) land well
# into the batch>=8 regime.
# PyPI only ships sdist (source build); use the matching prebuilt wheel from each
# project's GitHub Releases instead (pick the asset matching your torch/cuda/cp/abi):
#   https://github.com/Dao-AILab/causal-conv1d/releases  (this stack: causal-conv1d==1.7.0)
#   https://github.com/state-spaces/mamba/releases        (this stack: mamba-ssm==2.3.2.post1)
# mamba-ssm>=2.3 also pulls in tilelang==0.1.8 / apache-tvm-ffi<=0.1.9 / quack-kernels
# (all have matching wheels for cp312/x86_64). pip may resolve numpy up past the pin
# above (1.26.4) via that chain -- reinstall numpy==1.26.4 after if so; decord/opencv
# were confirmed still working under numpy 2.5.3 too, but stick to the pin regardless.
pydantic>=2.7,<3
markdown2[all]
httpx==0.24.1
requests
openai
uvicorn
fastapi>=0.110.0
tensorboard
wandb
tabulate
Levenshtein